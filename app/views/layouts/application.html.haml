!!! 5
%html
  %head>
    %title = @title || "Ticketee" 
    = stylesheet_link_tag    "application"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    - flash.each do |key, value|
      %div{:class => 'flash', :id => key}
        = value
    = yield
